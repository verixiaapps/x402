kind: Fixed
body: Budget SVM `upto` rent cleanup reclaim transactions per rent-payer signer instead of a shared pool, and resume a budget-limited scan from where the last pass stopped instead of always rescanning from the start
time: 2026-08-13T18:30:00.000000-04:00
